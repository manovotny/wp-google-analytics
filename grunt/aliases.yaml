default:
  - build

config:
  - replace
  - rename

build:
  - clean:js
  - browserify:admin
  - browserify:site

generate:
  - makepot
  - browserify

update:
  - clean:lib
  - shell:update
  - copy
  - curl

lint:
  - jshint
  - jslint
  - phplint

test:
  - phpunit

release:
  - lint
  - test
  - build
  - generate
  - replace:project_version